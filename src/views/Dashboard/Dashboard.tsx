import React from 'react';
import { useHistory } from "react-router-dom";

type LoginObj = {
  password: string,
  username: string,
}

export default function Dashboard() {
  let hist = useHistory();
  
  

  return (
    <div className="App">
      
    </div>
  );
}

